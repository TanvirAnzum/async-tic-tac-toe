import { useMutation } from '@tanstack/react-query'
import React from 'react'

const useFinishGame = ({initiator, opponent}) => {
 
  const {} = useMutation({mutationFn: () => })


  return (
    <div>useFinishGame</div>
  )
}

export default useFinishGame